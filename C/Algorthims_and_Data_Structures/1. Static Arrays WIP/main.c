#include <stdio.h>

// The number of values doesn't change
// Consecutive chunk of memory
// Operations supported: ;
// - build(x)
// - len()
// - iter_seq() - output x_0, x_1, x_n
// - get_at(i)
// - set_at(i)
// - get_first/last()
// - set_first/last


struct static_array{
    int size;
    int* arr_ptr;
};

int size(struct static_array* arr){
    for i in 
};



